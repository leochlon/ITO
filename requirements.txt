# ITO Pipeline - Information-Theoretic Optimization for SDXL
# Dependencies for image generation with adaptive guidance

# Core dependencies
torch>=2.0.0
diffusers>=0.35.0
transformers>=4.57.0
accelerate>=1.11.0
safetensors>=0.6.0

# Image processing and utilities
pillow>=12.0.0
numpy>=2.3.0

# Additional utilities
pyyaml>=6.0.0

# Note: Make sure your PyTorch installation matches your CUDA version
# For GPU support, install PyTorch with CUDA from: https://pytorch.org/get-started/locally/
# For Apple Silicon (MPS), the default PyTorch installation includes MPS support
